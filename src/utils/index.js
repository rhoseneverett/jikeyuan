// 统一中转工具模块函数
// import {request} from '@/utils'
import {setToken,getToken,removeToken} from './token'

import { request} from './request'

export {
  request,
  setToken,
  getToken,
  removeToken
}

